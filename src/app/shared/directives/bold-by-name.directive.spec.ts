import { BoldByNameDirective } from './bold-by-name.directive';

describe('BoldByNameDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldByNameDirective(null);
    expect(directive).toBeTruthy();
  });
});
